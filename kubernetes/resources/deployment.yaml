apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamwizard-backend-eventsub
spec:
  replicas: 5
  selector:
    matchLabels:
      app: streamwizard-backend-eventsub
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled  # This annotation enables automatic injection
      labels:
        app: streamwizard-backend-eventsub
    spec:
      containers:
        - name: streamwizard-backend-eventsub
          image: ghcr.io/streamerbuddybot/eventsub:904a79b84eb9d46295c1bf496173983fb8995387
          ports:
            - containerPort: 80
          envFrom:
            - secretRef:
                name: streamwizard-backend
          volumeMounts:
            - name: grpc-dns
              mountPath: /usr/src/app/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: grpc-dns
          configMap:
            name: grpc-dns
